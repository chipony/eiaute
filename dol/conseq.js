let badLen = true;  // For demonstration purposes

if (badLen) {
    // This block will be executed if badLen is true
    console.log("The length is bad!");
    // Additional code related to the bad length case
} else {
    // This block will be executed if badLen is false
    console.log("The length is good!");
    // Additional code related to the good length case
}
